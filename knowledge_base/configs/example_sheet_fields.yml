path: example_fields.xlsx
description: 這是範例知識庫，用來測試知識庫讀取XLSX表格與裡面欄位的功能。
# 一天
# update_delay_seconds: 86400
index_fields:
  - 水果
display_fields:
  - 肉類
auto_update:
  enable: true
  # delay_seconds: 86400
  delay_seconds: 3
index: 
  mode: last
  length: 100
  # max_tokens: 4096
