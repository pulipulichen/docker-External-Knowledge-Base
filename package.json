{
    "scripts": {
        "start": "sudo docker compose down; sudo docker compose up --build",
        "reset": "sudo docker compose down -v; sudo docker compose up --build",
        "bash": "sudo docker exec -it docker-external-knowledge-base-api-1 bash"
    }
}